
def step2_training(model_filename):

    # train a model here and save it under model_filename.yourextension, based on the training_set folder
    # this step is optionnal if you use an already trained model.

    return

def  step2_evaluation (model_filename):
    
    # evaluate the tags for the images in the test_folder and return them as a numpy array
    # see answer.npy for details about the structure
    
    return evluation
